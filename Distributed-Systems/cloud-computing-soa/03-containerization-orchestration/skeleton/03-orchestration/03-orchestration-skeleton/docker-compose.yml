# TODO:
# Create Docker Compose services for:
# - auth service
# - shortener service
#
# Expected guide points:
# 1) build each service from its own context directory
# 2) map ports 5001:5001 and 5000:5000
# 3) mount persistent data volumes to /app/data
# 4) mount keys folders to /app/keys
# 5) shortener depends_on auth
# 6) restart policy: unless-stopped
# 7) define named volumes auth_data and shortener_data

# version: "3.9"
#
# services:
#   auth:
#     build:
#       context: ./auth_service
#     container_name: auth_service
#     ports:
#       - "5001:5001"
#     volumes:
#       - auth_data:/app/data
#       - ./auth_service/keys:/app/keys
#     restart: unless-stopped
#
#   shortener:
#     build:
#       context: ./shortener_service
#     container_name: shortener_service
#     ports:
#       - "5000:5000"
#     volumes:
#       - shortener_data:/app/data
#       - ./shortener_service/keys:/app/keys
#     depends_on:
#       - auth
#     restart: unless-stopped
#
# volumes:
#   auth_data:
#   shortener_data:
