# TODO:
# Option A (recommended): build ConfigMap from auth public key file.
# kubectl -n websvc create configmap shortener-public-key \
#   --from-file=public_key.pem=auth_service/keys/public_key.pem \
#   -o yaml --dry-run=client | kubectl apply -f -
#
# Option B: inline PEM text manually.

# apiVersion: v1
# kind: ConfigMap
# metadata:
#   name: shortener-public-key
#   namespace: websvc
# data:
#   public_key.pem: |
#     -----BEGIN PUBLIC KEY-----
#     ...
#     -----END PUBLIC KEY-----
