# TODO:
# Option A (recommended): do NOT hardcode key data here.
# Create this secret from files:
# kubectl -n websvc create secret generic auth-keys \
#   --from-file=private_key.pem=auth_service/keys/private_key.pem \
#   --from-file=public_key.pem=auth_service/keys/public_key.pem \
#   -o yaml --dry-run=client | kubectl apply -f -
#
# Option B: fill base64 content manually (less recommended).

# apiVersion: v1
# kind: Secret
# metadata:
#   name: auth-keys
#   namespace: websvc
# type: Opaque
# data:
#   private_key.pem: <BASE64_PRIVATE_KEY>
#   public_key.pem: <BASE64_PUBLIC_KEY>
