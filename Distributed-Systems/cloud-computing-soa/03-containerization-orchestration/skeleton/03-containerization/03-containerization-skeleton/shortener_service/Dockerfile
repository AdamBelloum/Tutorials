# TODO:
# Use a compact Python base image (hint from guide: python:3.11-slim).
# FROM python:3.11-slim

# TODO:
# Set working directory inside container.
# WORKDIR /app

# TODO:
# Copy dependencies file and install requirements first for layer caching.
# COPY requirements.txt .
# RUN pip install --no-cache-dir -r requirements.txt

# TODO:
# Copy application code, keys, and data folders.
# NOTE: copy shortener.py (not auth.py).
# COPY shortener.py .
# COPY keys ./keys
# COPY data ./data

# TODO:
# Expose service port 5000.
# EXPOSE 5000

# TODO:
# Start the shortener service.
# CMD ["python", "shortener.py"]
